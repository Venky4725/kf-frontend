VITE_API_URL=https://kf-backend-1.onrender.com/api
Let me check the frontend API call. The issue is likely that the frontend needs to be updated to pass is_active=false. Can you check what API endpoint the frontend is calling for the archive page?

For now, let me verify the backend endpoint is working by checking the route:

Read file(s)

profiles.py 18 - 46
The backend is correctly set up. The issue is that the frontend needs to call:

GET /api/profiles?is_active=false
But it's probably calling just:

GET /api/profiles
The frontend code needs to be updated to add &is_active=false to the API call on the Archive page.

The backend is ready and working. The frontend just needs to pass the is_active=false parameter when fetching archived users.Let me check the frontend API call. The issue is likely that the frontend needs to be updated to pass is_active=false. Can you check what API endpoint the frontend is calling for the archive page?

For now, let me verify the backend endpoint is working by checking the route:

Read file(s)

profiles.py 18 - 46
The backend is correctly set up. The issue is that the frontend needs to call:

GET /api/profiles?is_active=false
But it's probably calling just:

GET /api/profiles
The frontend code needs to be updated to add &is_active=false to the API call on the Archive page.

The backend is ready and working. The frontend just needs to pass the is_active=false parameter when fetching archived users.